/**
 * 실제 타입은 `supabase gen types typescript --linked` 로 자동 생성하지만
 * Docker가 필요해서 PoC 단계에선 stub 유지. 운영 단계에서 교체.
 */
/* eslint-disable @typescript-eslint/no-explicit-any */
export type Database = any;
